# PDLC 状态机目录

此目录由 `pdlc-*` 命令自动维护，请**不要手动修改**。

## 文件格式

每个功能 ID 对应一个 `<feature-id>.json`，记录该功能经历的 PDLC 阶段、每阶段产出物、自检结果，以及下一跳命令。

## 是否纳入 Git

建议**纳入 git**（不加入 `.gitignore`）。状态机文件是功能开发过程的审计记录，便于 `/pdlc-retro` 出月度复盘。

## 使用

- `/pdlc-status` — 读取本目录出项目总览
- `/pdlc-retro` — 按时间范围读历史出趋势报告
