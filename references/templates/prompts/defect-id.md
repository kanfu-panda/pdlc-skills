<!-- 缺陷 ID 分配算法 · 被 fix.md @include -->

## 缺陷ID分配（必须执行）

1. 获取今日日期，格式 `YYYYMMDD`
2. 扫描 `docs/` 目录下所有文件名，匹配模式 `B<今日日期>-(\d{2})`，提取所有序号
3. 取最大序号 +1（两位补零），无匹配则为 `01`
4. 生成缺陷ID：`B<YYYYMMDD>-<NN>`
5. 从 Bug 描述中提取功能名关键词（英文小写+连字符）

**注意**：日期使用执行时的实际日期。
