# docs/00_standards/test-commands.yml — 本项目 check 命令的唯一真源
#
# pdlc-tdd / pdlc-implement / pdlc-review 与外层循环都从这里取「check」。
# 约定「退出码 0 = 通过」。

unit:     "bash scripts/unit.sh"
coverage: ""
lint:     "bash scripts/lint.sh"
e2e:      ""
